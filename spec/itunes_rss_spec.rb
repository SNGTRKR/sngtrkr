require 'spec_helper'
describe "ItunesRss" do

	context "when a new release is found" do
		it "ignores when the artist is not present" do
		end

		it "adds when the artist is present" do
		end

		it "associates the release with its artist" do
		end

		it "finds artwork from last.fm" do
		end
	end

	context "when there are no new releases" do
		it "does not add any releases" do
		end
	end

end