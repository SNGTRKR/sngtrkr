<% content_for :og_name, "Track your favourite artists and never miss another release..."
content_for :og_url, request.url
content_for :og_image, File.join(root_url , "/images/icon.png")
content_for :og_description, "Sign up free now to receive an exclusive beta release invite and get the latest updates. Login with Facebook to find artists you like. Listen to & buy tracks, with an easy to use timeline. Free notifications when artists release new tracks."
content_for :og_type, "website"
%>
<% content_for :title do %>Track your favourite artists and never miss another release...<% end %>
<% content_for :description do %>Sign up free now to receive an exclusive beta release invite and get the latest updates. Login with Facebook to find artists you like. Listen to & buy tracks, with an easy to use timeline. Free notifications when artists release new tracks.<% end %>
<script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.3.2/jquery.min.js" ></script>
<script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jqueryui/1.5.3/jquery-ui.min.js" ></script>
<script type="text/javascript">
	$(document).ready(function(){
		$("#featured > ul").tabs({fx:{opacity: "toggle"}}).tabs("rotate", 5000, true);
	});
</script>
<div class="row"><div class="span12">
<div class="row">
<div class="span5">
	<h1 class="home-header">Sign up free now to receive an exclusive BETA invite...</h1>
	<p class="home-txt-1">We will be releasing invites in waves. When the next wave is sent out, we will email you to let you know you can log in. </p>
	<div id="beta-form">
	<%= form_for(@beta_user) do |f| %>
      <%= f.text_field :email, :class => "required email form-text beta-input font-15", :placeholder => "Enter email address..." %>
      <%= f.submit "Sign Up", :id => "mc-embedded-subscribe", :class => "font-13" %>
       <% end %>
   </div>
</div>
<div class="span7">
	<%= image_tag "/images/home.png", :alt => "Macbook", :id => "home-macbook" %>
</div></div>
<div class="row">
<div class="span12">
<div id="featured" >
		  <ul class="ui-tabs-nav">
		  	<% @latest_releases.each_with_index do |release, i| %>
	        <li class="ui-tabs-nav-item ui-tabs-selected" id="nav-fragment-<%= (i+1) %>">
	        	<a href="#fragment-<%= (i+1) %>" <% if release == @latest_releases.first %> id="arrow-first" 
	        		<% elsif release == @latest_releases.last %> id="arrow-last" <% end %>>
	        	<h3><%= truncate release.artist.name, :length => 30 %> - <%= truncate release.name, :length => 25 %></h3>
	        	<p>Released on <%= release.date.strftime("#{release.date.day.ordinalize} %B") %></p>
		        </a>
		    </li>
	        <% end %>
	      </ul>

	    <% @latest_releases.each_with_index do |release, i| %>
	    <!-- <%= (i+1).ordinalize %> Content -->
	    <% if i==0 then c = "ui-tabs-panel" else c = "ui-tabs-panel ui-tabs-hide" end %>
	    <div id="fragment-<%= (i+1) %>" class="<%= c %>" style="">
		    <div class="release" style="background-image:url(<%= release.image :release_i %>);"></div>
			<div class="artist" style="background-image:url(<%= release.artist.image %>);">
				<div class="info" >
				<h4><%= link_to "#{truncate release.artist.name, :length => 30} - #{truncate release.name, :length => 25}", [release.artist, release] %></h4>
				<p>Released on <%= release.date.strftime("#{release.date.day.ordinalize} %B") %>
					<% if release.label_name? %>
						on <%= release.label_name %>
					<% end %>
				</p>
			 </div>
			</div>			 
	    </div>
	    <% end %>
		</div>
	</div>
</div>
<div class="row">
<div class="span12">
<div id="home-points">
	<div class="row">
	<div class="span4">
	<h2>Find artists you like</h2>
	<p>SNGTRKR intuitively recognises which artists you've liked on Facebook, and supplies you with recommendations to build up your tracked artists.</p>
	<%= image_tag "/images/home1.png", :alt => 'Recommendations', :id => 'home1' %></div>
	<div class="span4"><h2>Keep up to date</h2>
	<p>Stay in the loop with <strong>your personalised timeline</strong> of artist releases, old and new. We'll email you whenever an artist you follow releases a new album or single.</p>
	<%= image_tag "/images/home3.png", :alt => 'Timeline', :id => 'home3' %></div>
	<div class="span4 discover"><h2>Discover new music</h2>
	<p>Looking to expand your music library? SNGTRKR makes it easy to discover new music with user ratings, track previews, similar artists and purchase links.</p>
	<%= image_tag "/images/home2.png", :alt => 'Music', :id => 'home2' %></div>
<div class="clear"></div>
	</div></div>
</div></div></div></div>

