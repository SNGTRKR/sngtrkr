<% if @timeline.empty? %>
    console.log("No more timeline artists");
<% else %>
    $('.user_timeline').append('<% @timeline.each do |release| %><%=j render :partial => "timeline/release", :object => release, :locals => { :release_type => "", :tracking_category => "timeline", :tracking_name => "user-timeline" } %><% end %>');
<% end %>
$('.loading_block').remove();