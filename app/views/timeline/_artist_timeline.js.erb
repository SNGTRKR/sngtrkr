<% if @timeline.empty? %>
    console.log("No more timeline artists");
<% else %>
    $('.artist_timeline').append('<% @timeline.each do |release| %><%=j render :partial => "timeline/release", :object => release, :locals => { :release_type => "", :tracking_category => "artist", :tracking_name => "artist-timeline" } %><% end %>');
<% end %>
$('.loading_block').remove();