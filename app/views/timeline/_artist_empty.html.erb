<div id="timeline-empty" class="recommendations">
	<h1>Oh dear...</h1>
	<p class="empty-message">It appears <%= @artist.name %> have not released anything or we were unable to acquire anything from our partner stores. Why not check out some other artists which may be of interest to you?
	</p>
	<% if !user_signed_in? %><% else %><%= render 'timeline/empty' %><% end %>
</div>
